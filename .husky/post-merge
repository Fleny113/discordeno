if git diff-tree -r --name-only --no-commit-id HEAD@{1} HEAD | grep -q "pnpm-lock.yaml\|package.json"; then
  echo "hook(post-merge) - Found changes to a pnpm lockfile or package.json, running pnpm install."
  pnpm install
fi
